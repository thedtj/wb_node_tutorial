mixin forgotForm()

	form.form(action="/account/forgot" method="POST")
		h2 Forgot yo' password?
		label(for='email') Email
		input(type="email" name="email")
		input.button(type="submit" value="Send Reset Request")